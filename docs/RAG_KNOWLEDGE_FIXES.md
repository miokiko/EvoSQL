# RAG 知识内容修正（2026-09-05）

本页记录第一轮修正。后续 DDL 复核已更新字段解释、默认口径和 12 条关系；当前结论请看 [DDL_BUSINESS_INTERPRETATION.md](DDL_BUSINESS_INTERPRETATION.md)。以下数量、状态和索引版本均为第一轮历史记录。

第一轮保留数据库快照、原始 SQL dump 和当时待确认的关系。

## 内容修正

- 项目原始值修正为 `Neelum-Jhelum水电站引水隧洞`。
- 支护原始类别保留为单个值 `钢拱架/TH梁`，不拆成两个枚举。
- 区分分类表案例编码数 36 与主档已分类案例数 35；“强烈”分别为 6 和 5。文档明确统计范围，不自动批准跨表关系。
- 补充附件、监测汇总和时间序列中案例编码对主档的覆盖范围。
- 修正空字符串含义、非空观测值与 NULL 的区分，以及缺失值过滤和分组规则。
- 将“暂不进入业务检索”改为准确的 Schema 收录与业务解释边界。

## 生成与使用方式

- `knowledge/schema_annotations.json` 标记 14 个有问题或含义待确认的原始注释，其中 6 个混入支护材料列表。检索文本显示“注释待确认”；原始注释保留在快照和语料结构化字段 `raw_comment` 中。
- 102 组观测值使用 JSON 数组保留原始类别边界，另列 NULL 行数。
- 业务页面用 `knowledge_status` 区分观测事实、候选解释、已确认口径和默认未确认状态。当前混合业务解释的页面均标记为候选，不新增 PR 审批流程。
- 18 个业务术语片段都提供 `<!-- planning -->` 纯业务说明。完整正文供 Schema Grounding 使用，规划角色读取对应说明；同一证据保留来源版本和内容状态。
- 规划说明在构建时和运行时检查物理表名、字段名和 SQL，保持原有隔离边界。
- 业务文档仍为 25 个片段，索引总量仍为 405：386 条 documentation、18 条 DDL、1 条 SQL 示例。

## 验证

新增回归检查覆盖：业务说明传递、Schema/SQL 泄漏阻断、注释隔离与溯源、注释清单快照漂移、值域类别边界、缺失状态和内容状态。

```bash
python -m unittest discover -s tests -p 'test_*.py'
python scripts/build_text2sql_vanna.py
```

234 项 Python 回归检查通过。重建后的索引版本为 `vanna-7a83e84cf5e7`；应在后端重新加载代码后使用。当前没有用真实模型跑完整问答评测，回归通过不等于 SQL 问答准确率已得到提升幅度。

## 第一轮结束时保留的待核查内容

14 个字段的真实业务名称或单位、主档范围与分类表范围的默认选择、新旧支护表关系、等级顺序、时间解析规则和候选连接，均未凭猜测改为已确认结论。相关文档保留其限制。
